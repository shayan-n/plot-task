export default function Plot({
    width, height
}) {
    return (
        <svg 
            width={width} 
            height={height}
            className="bg-green-500"
        >
        </svg>
    );
}